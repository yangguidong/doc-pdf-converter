# ============================================================
# 3D相机多角度图像生成器 - 依赖 (纯免费方案)
# ============================================================

# 核心UI
gradio>=4.0.0

# 调用 HF Space API (免费)
gradio_client

# HTTP 请求 (备用 Colab 后端)
requests

# 图像处理
pillow
numpy

# 环境变量
python-dotenv
